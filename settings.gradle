pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Additional
        jcenter()
        maven {
            url 'https://dl.bintray.com/amulyakhare/maven'
        }
    }
}
rootProject.name = "NomNomFood"
include ':app'
